dump_append_exp_name: true
dump_base_dir: ${paths.dump_dir}
cli:
  --attention-dropout: 0.0
  --hidden-dropout: 0.0
  --accumulate-allreduce-grads-in-fp32: true
  --attention-softmax-in-fp32: true
  --attention-backend: flash
