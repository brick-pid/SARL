repo_dir: ${hydra:runtime.cwd}
model_dir: ${paths.repo_dir}/models
data_dir: ${paths.repo_dir}/data
checkpoint_dir: ${paths.repo_dir}/experiments/checkpoints
dump_dir: ${paths.repo_dir}/dump
exp_name: ${custom.env_name}-${model.label}-${algo.name}-${custom.name}-${now:%H%M}
exp_dir: ${paths.repo_dir}/experiments/runs/${now:%Y%m%d}/${paths.exp_name}
